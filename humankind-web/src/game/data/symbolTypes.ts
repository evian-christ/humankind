/** 심볼 시대·분류 — `symbolDefinitions`와 후보 데이터 간 순환 참조를 피하기 위해 분리 */
export enum SymbolType {
    RELIGION = 0,
    RESOURCE = 1,
    MEDIEVAL = 2,
    MODERN = 3,
    TERRAIN = 4,
    ANCIENT = 5,
    DISASTER = 8,
    SPECIAL = 9,
    LUXURY = 10
}
