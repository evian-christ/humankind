# HUMANKIND - 게임 디자인 레퍼런스 및 방향성 (Game Design Reference)

*본 문서는 반복적인 플레이 및 코드 구현 상태를 바탕으로 유지하는 게임 디자인 문서입니다. 구현 세부는 소스 코드가 최종 기준입니다.*

---

## 용어 표기 (Terminology)
- 본 프로젝트의 진행 단위 표기는 **“턴”**으로 통일합니다. (과거 문서/표기에서 사용하던 “스핀(Spin)”은 동일 의미로 취급하며 문서에서는 사용하지 않습니다.)

## 1. 개요 (Overview)
- **장르**: 슬롯머신 기반 덱빌딩 생존 전략 (Slot-builder Deckbuilding Strategy)
- **핵심 목표**: 매 주기 부과되는 식량(Food) 요구량을 충족하여 생존하고, 지식(Knowledge)을 모아 시대를 발전시키며, 같은 슬롯 보드 위에서 시대마다 다른 생산 문법을 다루는 것.
- **주요 테마**: 인류 문명의 발전 (고대의 직접 생산/소모 👉 중세의 증폭/전환/재가공 👉 현대의 연쇄/전역 스케일링).

---

## 2. 핵심 자원 및 컨트롤 (Core Resources)
게임 내 핵심 생산 자원은 식량, 골드, 지식 3종입니다.
- **식량 (Food - 초록색 오각형)**: **생존의 핵심 자원.** 매 10턴마다 모든 게임에 동일한 요구량 곡선을 적용합니다. 요구량은 `20 → 50 → 100 → 170 → 260…`이며 증가폭은 `+30, +50, +70, +90…`로 납부마다 20씩 커집니다. 보드 크기와 무관하며, 납부 페이즈에서 지불하지 못하면 게임 오버. 납부 성공은 보드 확장권과도 연결됩니다.
- **골드 (Gold - 노란색 원형)**: **선택 관리 자원.** 심볼 선택 단계에서 리롤 비용으로 사용됩니다.
- **지식 (Knowledge - 파란색 빛)**: **성장 자원.** 지식 게이지가 찰 때마다 레벨업하며 연구 포인트 1개를 얻습니다. 연구 포인트는 `시대 / 수렵 / 목축 / 농업 / 어업 / 무역 / 열대농경 / 학문 / 신앙` 중 현재 레벨 조건을 만족한 한 분야의 다음 단계를 연구하는 데 사용합니다. 레벨 10의 중세시대, 레벨 20의 현대시대, 레벨 30의 AGI 프로젝트는 `시대` 분야의 연속 단계입니다.

---

## 3. 게임플레이 코어 루프 (Gameplay Core Loop)
1. **턴 (Turn)**: 턴 버튼을 누르면 보유 심볼이 현재 보드 모양 안에 무작위 배치되고, 심볼 효과가 순서대로 처리됩니다.
2. **선택 (Selection)**: 제시된 선택지 중 내 덱의 시너지에 맞는 1개를 고릅니다. 기본은 3장이나 재해 등에 의해 2장으로 줄 수 있고, 심볼 대신 이벤트 카드가 섞일 수 있습니다. 건너뛰기 혹은 골드 지불로 리롤 가능합니다.
3. **생존 확인 및 보드 확장 (Survival & Expansion)**: 10턴마다 `food_payment` 페이즈에서 요구 식량을 지불합니다. 성공하면 다음 주기로 넘어가며 보드 확장권을 얻고, 실패하면 게임 오버입니다.

### 3-0. 플레이어 의사결정과 랜덤 배치
- 보드 위 실제 위치는 매 턴 플레이어 보유 심볼이 무작위로 배치된 결과이며, 플레이어는 특정 심볼을 특정 칸에 놓거나 원하는 인접 구조를 직접 만들 수 없다.
- 따라서 인접, 행/열, 코너, 빈칸 조건은 "배치 퍼즐"이 아니라, 플레이어가 선택한 심볼 구성이 랜덤 배치에서 얼마나 자주 유리한 상황을 만드는지에 대한 확률적 시너지다.
- 설계 문서와 효과 설명에서는 "플레이어가 배치한다", "주변을 채운다", "특정 위치에 놓는다"처럼 직접 배치 조작을 암시하는 표현을 피한다.

### 3-1. 시대가 바꾸지 않는 것 / 바꾸는 것
- **바꾸지 않는 것**: 슬롯 보드, 심볼/이벤트 선택, 식량/골드/지식 생산, 10턴 주기 식량 납부, 보드 확장, 지식 업그레이드, 시대 전환.
- **바꾸는 것**: 플레이어가 같은 자원을 어떤 방식으로 생산하고 증폭하고 전환하는지에 대한 **생산 문법**.
- 시대 차이는 새로운 미니게임을 덧붙여 만드는 것이 아니라, 같은 코어 루프 안에서 심볼 효과의 성격을 달리해 만드는 것이다.

### 3-2. 턴 처리 구현 기준
- 턴은 **계산 결과**와 **연출 타임라인**을 분리해서 다룬다.
- 계산 파이프라인은 슬롯 순서, 심볼 효과, 누적 자원, 생성/파괴 결과를 확정한다.
- 연출 타임라인은 효과 속도 설정에 따른 phase 1/2/3 지연과 후처리 pause를 담당한다.
- `setTimeout` 기반 연출 콜백은 run id/cancel token을 통해 현재 턴 run이 아니면 실행되지 않아야 한다.
- 즉, 게임 규칙의 정답은 계산 레이어가 만들고, Pixi/타이머는 그 결과를 보여주는 역할에 머문다.

---

## 4. 메인 메커니즘 및 덱 빌딩 시너지 (Mechanics & Synergies)
단순히 수치가 높은 심볼을 모으는 것을 넘어, 보드 위에서의 상호작용이 핵심입니다.

- **지형 중심 덱빌딩 (Terrain-Centric Deckbuilding)**:
  - 이 게임에서 지형 심볼은 단순 보조 파츠가 아니라, 문명 게임에서의 "영토"에 가까운 핵심 기반입니다.
  - 플레이어는 고대 시대에 확보한 지형을 바탕으로 이후 시대의 운영 방향을 결정하며, 시대가 진행될수록 지형 심볼은 희소해져 기존 기반을 더 값지게 활용해야 합니다.
  - 메인 덱은 자원 종류가 아니라 **지형 기반 운영 방식**으로 구분됩니다. 현재 기준 메인 덱 후보는 `초원`, `평원`, `바다`, `숲`, `열대우림`, `사막`이며, `산`은 특수 포지션으로 취급합니다.
- **공통 생존층 + 열린 빌드 (Shared Survival Layer + Open Build)**:
  - 따라서 플레이는 하나의 직업형 덱을 고르는 방식이 아니라, 여러 지형 기반 덱을 혼합하면서 각 자원을 자신만의 방식으로 수급하는 열린 빌드를 지향합니다.
- **위치 기반 인접 시너지 (Adjacency Bonus)**: 
  - 특정 심볼은 인접한 위치(주변 8칸)의 다른 심볼에 큰 버프를 줍니다.
  - 단, 이 인접은 플레이어가 직접 배치해서 만드는 퍼즐이 아니라 매 턴 무작위 배치에서 발생하는 확률적 결과입니다.
  - *예시*: `초원`은 `밀`과 `쌀`의 카운터를 더 빠르게 올리고, `평원`은 `소`와 `말`의 식량 생산을 강화합니다. `바다`는 `물고기`, `게`, `진주`의 핵심 기반이 됩니다.
- **자기 파괴와 변이 (Destroy & Mutation)**: 
  - 영구히 자리를 차지하는 심볼 외에도, 일정 턴 이후 효과를 내고 스스로 사라지는 일회성/조건부 심볼들이 보드의 흐름을 원활하게 합니다.
- **이벤트 선택지 (Events)**:
  - 일반 심볼 선택지에는 확률적으로 이벤트 카드가 섞일 수 있습니다.
  - 이벤트는 즉시 자원 보상, 지형 보유량 조건부 보상, 위험 감수 보상 등으로 구성됩니다.
  - 일부 업그레이드는 이벤트 등장 확률을 높이거나, 다음 선택지에 이벤트를 강제합니다.
- **재해 (Disasters)**:
  - 자연재해는 턴 준비 단계에서 발생하는 공통 압박층입니다.
  - 재해 심볼은 생산 방해, 선택지 감소, 선택 페이즈 비활성화, 열 파괴 등으로 경제 엔진에 압박을 줍니다.
- **시대별 생산 문법 (Era-specific Production Grammar)**:
  - 각 시대는 같은 식량/골드/지식 자원을 다루되, **어떤 방식으로 값을 만들어내는가**가 달라져야 한다.
  - **고대**는 직접 생산, 카운터 누적, 파괴 시 보상, 단순 인접 시너지의 비중이 높다.
  - **중세**는 기존 생산을 증폭하거나 다른 자원으로 전환하고, 랜덤 배치에서 발생한 인접/행/열/전역 조건을 더 크게 해석하는 비중이 높다.
  - **현대**는 여러 심볼의 결과를 연쇄시키고, 보드 전역 상태를 참조해 생산을 크게 키우는 비중이 높다.
- **지식 업그레이드 (Knowledge Upgrades)**: 
  - 보드 전체의 법칙을 바꾸는 강력한 시스템. 단순한 수치 뻥튀기가 아닌 덱의 구조를 바꿀 수 있습니다 (예: 특정 심볼의 추가 소환, 종교 시스템 해방 등).
  - 가로 기술 트리와 카드 간 연결선은 사용하지 않습니다. 아홉 업그레이드 카드는 항상 같은 자리에 남고, 연구할 때 해당 카드의 레벨이 올라갑니다.
  - 카드 제목은 `농업`, `농업 I`, `농업 II`처럼 고정 이름 뒤에 로마 숫자만 붙입니다. 기존 기술명은 효과 호환을 위한 내부 데이터로만 유지하고 화면에는 노출하지 않습니다.
- **보드 확장 (Board Expansion)**:
  - 현재 기본 보드는 3×2에서 시작하고, 고대 해금/봉건제/현대 업그레이드 및 10턴 식량 납부 성공을 통해 확장권을 얻습니다.
  - 플레이어는 보드의 기존 활성 슬롯에 인접한 후보 칸을 골라 보드 모양을 확장합니다.

### 4-1. 메인 지형덱 방향성 요약
- **초원덱**: 초원과 같은 가로줄에 있는 `밀`/`옥수수`/`쌀`의 식량 생산 주기를 줄이는 안정 식량축. 가로로 긴 보드를 선호하며, 골드와 지식은 외부에서 보충해야 한다.
- **평원덱**: 평원과 같은 세로줄에 있는 `소`/`양`/`말`이 추가 생산을 하는 다목적 축. 세로로 긴 보드를 선호하며, 식량 안정성과 골드 생산을 함께 가져가지만 순수 생산량은 과하지 않게 유지한다.
- **바다덱**: 가장자리의 바다는 `해안`, 내부의 바다는 `해양`으로 취급한다. 해안은 생존 식량, 해양은 지식과 희귀 리턴을 맡기며, 초반에는 외곽 위주 보드에서 중반 이후 내부를 일부 여는 운영을 목표로 한다.
- **숲덱**: 숲 군집과 숲 보유량이 식량 생산의 중심이다. 타 심볼보다 `숲` 자체가 주 생산자이며, 모든 방향으로 넓은 보드를 선호한다. 연결 심볼과 업그레이드 방향은 주요 브레인스토밍 과제다.
- **열대우림덱**: 각 열대우림이 개별 성장치를 쌓고, 성장치 10을 만든 성장 종류에 따라 식량/골드/지식 생산량이 증가하는 장기 성장축. 개별로 크게 성장한 열대우림의 가치와 재해 리스크가 모두 크다.
- **사막덱**: 인접한 자원/시대 심볼을 파괴하고, 파괴된 심볼이 큰 보상을 주는 불안정 고위험 축. `오아시스`는 빈칸 수에 따라 식량을 생산하며, 사막/오아시스 외 지형이 없을 때 식량 납부 후 보드 확장을 추가로 받아 빠르게 넓어지는 운영을 한다.

---

## 5. UI / UX 및 비주얼 디렉션 (UI & Visual Direction)
- **프리미엄 픽셀 아트**: 깔끔한 도트 그래픽을 기반으로, 칙칙하지 않고 고급스러우며 동적인 연출 추구.
- **몰입도를 높이는 박물관 감성**: 문명사 자료를 전시하는 듯한 절제된 프레임과 조명을 사용합니다.
- **쾌적한 애니메이션 피드백**:
  - 턴 후 자원이 날아가는 연출, 레벨업 시 화면의 전환 처리 등이 매끄러우며, 짧은 호흡의 아케이드적 느낌을 극대화.
- **데이터 브라우저**: 심볼, 업그레이드, 이벤트, 보상, 상태 데이터를 개발 중 빠르게 확인할 수 있어야 합니다.

### 5-1. Pixi 렌더링 구현 기준
- `PixiGameApp`는 Pixi 앱 생명주기, 컨테이너 순서, ticker, React/Zustand와의 연결을 조율한다.
- 현재 주요 HUD와 업그레이드 화면은 React UI가 담당하고, Pixi 쪽 HUD/업그레이드 렌더러는 캔버스 상태 및 hover 연동 어댑터 역할을 한다.

---

### 5-2. 기본 버튼 디자인 (DOS 스타일, 2026-07 확정)

- **배경**: 불투명 `background-color: #1c1c1c` + `background-image: linear-gradient(rgba(0,0,0,0.09), rgba(0,0,0,0.09))`. 반투명 `rgba` 배경 단독 사용 금지(과거 청동 팔레트 시절 방식이며 현재는 대체됨).
- **테두리 — 이중 구조**:
  - 버튼 자체: `border: 2px solid #0e0e0e`, `border-radius: 0`.
  - `::after` 의사요소로 내부 인셋 테두리를 하나 더 그린다: `content: ""`, `position: absolute`, `inset: 4px`, `z-index: 0`, `box-sizing: border-box`, `border: 2px solid #0e0e0e`, `box-shadow: none`, `pointer-events: none`. 버튼 본체는 `position: relative; isolation: isolate;`가 필요.
  - 아이콘/텍스트 등 실제 내용물은 `position: relative; z-index: 1;`로 인셋 테두리 위에 뜨도록 한다.
- **텍스트**: `color: rgba(255, 255, 255, 0.88)`, `text-shadow: none`. 폰트는 `var(--game-font-family), sans-serif` 공통.
- **box-shadow**: 항상 `none`. 그림자/글로우 장식 금지(DOS 플랫 원칙).
- **상태별 배경색** (테두리·그라데이션 오버레이는 유지, `background-color`만 교체):
  - 기본: `#1c1c1c`
  - hover / focus-visible: `#2e2e2e`
  - active: `#0e0e0e`
- **커서**: `cursor: var(--app-cursor-pointer)`.
- **금지 사항**: `border-radius` 0이 아닌 값, 그라데이션 색 변경(항상 검정 계열 9% 오버레이), 4면이 다른 색인 베벨, 갈색/청동/아이보리 색조(→ [[game-ui-layout]] 이전의 팔레트, 현재는 검정+회색 램프로 전면 교체됨).


### 5-4. 기본 UI 구현 원칙 (미니멀 우선)
- 새로운 오버레이/모달/패널은 사용자가 “스타일을 화려하게” 요청하기 전까지 **초미니멀** 형태로 먼저 구현한다.
- 초미니멀 기본 레이아웃 규격:
  - 배경: `rgba(0,0,0,0.6~0.8)` 같은 단색 반투명
  - 본문: 단순 `border` 또는 얇은 구획선(둥근 모서리/그라데이션/강한 그림자 최소화)
  - 타이포: 통일된 폰트 + 과도한 텍스트 효과(획/그림자/입체효과) 지양
  - 동작: 클릭 배경 닫기 + `Esc` 닫기(가능하면) 같은 기본 UX만 포함
- 버튼 자체의 색/테두리/그림자 스펙은 5-2를 따른다(패널 배경의 반투명 규칙과는 별개).
- 이후 단계에서만 요청/가이드가 있을 때:
  - 박물관 감성(조명/포인트 컬러/디테일 프레임), 프리미엄 그림자/그라데이션, 애니메이션을 점진적으로 추가한다.

## 6. 향후 확장 및 구현 목표 (Future Roadmap)

### 6-1. 시대 확장 (Era Expansion)
- (메모) 구현 범위는 반복 개발로 계속 변동될 수 있음.
- 각 시대는 새로운 코어 시스템을 추가하기보다, 같은 코어 루프 안에서 **생산 문법의 무게중심을 이동시키는 것**을 목표로 함:
  - **고대 (직접 생산 / 소모 / 카운터 축적)**:
    - 지형 인접, 일정 턴 누적, 파괴 시 보상 같은 즉물적인 생산 방식이 중심이다.
    - 플레이어는 어떤 지형과 타이머 심볼을 덱에 확보할지에 집중한다.
  - **중세 (증폭 / 전환 / 재가공)**:
    - 이미 깔린 지형과 기존 생산 엔진을 바탕으로, 생산량을 키우거나 다른 자원으로 바꾸거나 더 구조적으로 재해석하는 시대다.
    - 핵심은 새 자원을 만드는 것이 아니라, `직접 생산` 비중을 줄이고 `증폭`, `허브`, `중계`, `재활용`, `자원 전환` 비중을 높여 플레이 감각을 바꾸는 것이다.
    - 중세 심볼은 가능하면 단순 고수치 생산보다, 기존 심볼의 결과를 건드리는 효과를 우선한다.
  - **현대 (연쇄 / 전역 스케일링 / 자동화 감각)**:
    - 중세의 증폭과 전환 위에서, 여러 심볼의 결과가 이어지거나 보드 전체 상태를 참조하는 대규모 생산이 중심이 된다.
    - 현대는 중세보다 더 넓은 범위와 더 큰 규모의 계산을 담당하며, 시대 후반의 폭발력을 맡는다.

### 6-2. 다양성 (Diversity)
- 장기적으로 심볼 풀을 크게 확장해, 로그라이크 덱빌더 특유의 매 판 다른 조합(Replayability)의 재미를 증대.
- 단, 덱 구조는 폐쇄적인 직업형 덱보다 여러 지형 기반 엔진을 창의적으로 섞어 쓰는 방향을 우선한다.
- 특히 시대별 신규 심볼은 "새 시스템 추가"보다 "기존 생산 문법을 어떻게 바꾸는가"를 기준으로 평가한다.
